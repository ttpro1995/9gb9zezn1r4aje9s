<?xml version="1.0" encoding="utf-8"?>
<menu xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto">

    <item android:title="NHÓM">
        <menu>
            <item
                android:id="@+id/circle_list"
                app:actionLayout="@layout/widget_recycler_view" />
            <item
                android:id="@+id/circle_panel"
                app:actionLayout="@layout/widget_circle_panel" />
            <item
                android:id="@+id/place_list"
                android:icon="@drawable/ic_appointment"
                android:title="Địa điểm" />
            <item
                android:id="@+id/appointment_list"
                android:icon="@drawable/ic_appointment"
                android:title="Điểm hẹn" />
        </menu>
    </item>

    <item android:title="ĐANG PHÁT TRIỂN">
        <menu>
            <item
                android:id="@+id/profile"
                android:icon="@drawable/ic_personal"
                android:title="Cá nhân" />
            <item
                android:id="@+id/friend_list"
                android:icon="@drawable/ic_friend"
                android:title="Bạn bè" />
            <item
                android:id="@+id/timeline"
                android:icon="@drawable/ic_timeline"
                android:title="Dòng thời gian" />
            <item
                android:id="@+id/logout"
                android:icon="@drawable/ic_logout"
                android:title="Đăng xuất" />
        </menu>
    </item>

    <group android:id="@+id/main">
        <item
            android:id="@+id/inbox"
            android:icon="@drawable/ic_message"
            android:title="Tin nhắn" />
        <item
            android:id="@+id/settings"
            android:icon="@drawable/ic_settings"
            android:title="Cài đặt" />
    </group>

</menu>
